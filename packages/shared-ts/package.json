{
  "name": "@navix/shared-ts",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-contracts.mjs",
    "type-check": "pnpm -w tsc -p packages/shared-ts/tsconfig.json --noEmit",
    "lint": "pnpm -w eslint --config eslint.config.ts packages/shared-ts/src --max-warnings=0",
    "check": "pnpm type-check && pnpm lint",
    "format": "pnpm -w prettier --write packages/shared-ts/src/"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
