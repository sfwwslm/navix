{
  "name": "@navix/shared-ui",
  "version": "0.1.0-alpha.1",
  "private": true,
  "type": "module",
  "scripts": {
    "type-check": "pnpm exec tsc -p tsconfig.json --noEmit",
    "lint": "pnpm exec eslint --config ../../eslint.config.ts src --max-warnings=0",
    "check": "pnpm run type-check && pnpm run lint",
    "format": "pnpm exec prettier --write src/"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:"
  },
  "peerDependencies": {
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "dependencies": {
    "react": "catalog:",
    "react-dom": "catalog:"
  }
}
